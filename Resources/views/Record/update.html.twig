{% extends "HolloBindBundle::base.html.twig" %}

{% block body %}
<h2>Update record</h2>

{% include "HolloBindBundle:Record:info.html.twig" %}

<form method="post" action="{{ path('hollo_bind_record_update', {'id':record.id}) }}">
  <fieldset>
    <legend>Record</legend>
    {{ form_widget(form) }}

    <input type="submit" />
  </fieldset>
</form>

{% endblock %}
