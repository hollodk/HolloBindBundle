{% extends "HolloBindBundle::base.html.twig" %}

{% block body %}
<h2>Domains</h2>

<table>
  <thead>
    <tr>
      <th>Domain</th>
      <th>Address</th>
      <th>Updated at</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    {% for domain in domains %}
    <tr>
      <td>{{ domain.domain }}</td>
      <td>{{ domain.address }}</td>
      <td>{{ domain.updatedAt|date('Y-m-d H:i:s') }}</td>
      <td>
        <a href="{{ path('hollo_bind_domain_index', {'id':domain.id}) }}">Records</a>
        <a href="{{ path('hollo_bind_admindomain_update', {'id':domain.id}) }}">Edit</a>
        <a href="{{ path('hollo_bind_admindomain_delete', {'id':domain.id}) }}">Delete</a>
      </td>
    </tr>
    {% else %}
    <tr>
      <td colspan="4">There is no domains at the time.</td>
    </tr>
    {% endfor %}
  </tbody>
</table>

{% endblock %}
